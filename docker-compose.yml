services:
  web:
    build:
      context: .
    ports:
      - "8501:8501"
    environment:
      MODEL_BUNDLE_PATH: /app/artifacts/model.joblib
    volumes:
      - ./artifacts:/app/artifacts:ro
    restart: unless-stopped
